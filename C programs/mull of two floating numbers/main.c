#include <stdio.h>
int main()
{
    float num1,num2,mull;
    printf("enter two numbers");
    scanf("%f %f",&num1, &num2);
    mull=num1*num2;
    printf("multiplication of two number is %f", mull);
    return 0;
}
